{
  "name": "json-server",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:json-server": "json-server --watch db.json --port 3000",
    "start:server": "ts-node src/index.ts",
    "start": "concurrently \"npm run start:json-server\" \"npm run start:server\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.21.1",
    "json-server": "^1.0.0-beta.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.7.6",
    "concurrently": "^9.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
